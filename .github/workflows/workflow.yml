name: workflow
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: clone
        uses: actions/checkout@v4

      - name: setup
        uses: make setup

      - name: test
        uses: make test

      - name: lint
        uses: make lint

